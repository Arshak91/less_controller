export enum CompanyDirectionType {
    distributor = 1,
    manufacturer,
    trucking,
    logistics,
    other
};

export enum JobFunctionType {
    executive = 1,
    it,
    partner,
    sales,
    logistics,
    generalAdministration,
    planning
}